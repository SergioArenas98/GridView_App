# API contract

This directory will contain the GridView API contract:

- `gridview-api-v1.yaml` - OpenAPI specification for GridView API v1.

The contract is created in Phase 2 of
`../technical/GridView_Implementation_Plan.md` and is the source of truth
between the Flutter application and the edge API.
